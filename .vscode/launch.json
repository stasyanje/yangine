{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/cmake/win/app.exe",
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "make build"
        }
    ]
}