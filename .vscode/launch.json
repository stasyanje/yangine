{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/cmake/build/app.exe",
            "cwd": "${workspaceFolder}/cmake/build"
        }
    ]
}